<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Setup | V.S.B. Engineering College</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&family=Plus+Jakarta+Sans:wght@300;400;600&display=swap');
        
        * {
            cursor: none !important;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background: radial-gradient(circle at center, #0B001A 0%, #03000a 100%);
            color: #f3f4f6;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .heading-font {
            font-family: 'Orbitron', sans-serif;
        }

        .galaxy-card {
            background: rgba(13, 8, 28, 0.85);
            border: 1px solid rgba(139, 92, 246, 0.25);
            backdrop-filter: blur(20px);
            box-shadow: 0 0 30px rgba(139, 92, 246, 0.15);
        }

        /* Custom Cosmic Cursor */
        .custom-cursor {
            width: 14px;
            height: 14px;
            background: #a855f7;
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 10000;
            transform: translate(-50%, -50%);
            box-shadow: 0 0 10px #a855f7, 0 0 20px #2563eb;
            transition: transform 0.08s ease-out;
        }

        @media (max-width: 640px) {
            .custom-cursor {
                display: none !important;
            }
            * {
                cursor: auto !important;
            }
        }
    </style>
</head>
<body class="p-4 sm:p-6">

    <!-- Active Custom Cursor -->
    <div id="cursor-core" class="custom-cursor"></div>

    <div class="w-full max-w-md galaxy-card p-6 sm:p-8 rounded-3xl">
        <div class="text-center mb-6 sm:mb-8">
            <div class="mb-4 flex justify-center">
                <img src="/images/vsb-logo.png" alt="V.S.B. Logo" class="w-12 h-12 sm:w-16 sm:h-16 object-contain">
            </div>
            <h1 class="heading-font text-lg sm:text-xl font-bold tracking-wide">COMPLETE SETUP</h1>
            <p class="text-gray-400 text-[10px] sm:text-xs mt-1">V.S.B. Engineering College</p>
        </div>

        <!-- Alert Cards -->
        <% if (error) { %>
            <div class="bg-red-950/50 border border-red-500/30 text-red-300 text-xs p-3 rounded-lg mb-4 sm:mb-6">
                <%= error %>
            </div>
        <% } %>

        <!-- Client-Side Mismatch Alert Box -->
        <div id="register-error-box" class="hidden bg-red-950/50 border border-red-500/30 text-red-300 text-xs p-3 rounded-lg mb-4 sm:mb-6">
            Passwords do not match. Please verify.
        </div>

        <!-- Setup Form -->
        <form id="form-setup" action="/register/google-complete" method="POST" class="space-y-4 text-xs sm:text-sm">
            <div>
                <label class="block text-[10px] uppercase text-gray-400 font-semibold tracking-wider mb-2">Verified Google Email</label>
                <input type="email" disabled value="<%= email %>" class="w-full bg-white/5 border border-white/5 rounded-xl px-4 py-3 text-gray-400 focus:outline-none">
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-[10px] uppercase text-gray-400 font-semibold tracking-wider mb-2">Academic Year</label>
                    <select name="year" required class="w-full bg-[#110b24] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-cyan-400 transition">
                        <option value="1st year">1st year</option>
                        <option value="2nd year">2nd year</option>
                        <option value="3rd year">3rd year</option>
                        <option value="4th year">4th year</option>
                    </select>
                </div>
                <div>
                    <label class="block text-[10px] uppercase text-gray-400 font-semibold tracking-wider mb-2">Department</label>
                    <select name="department" required class="w-full bg-[#110b24] border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-cyan-400 transition">
                        <option value="CSE">CSE</option>
                        <option value="CSE(AIML)">CSE(AIML)</option>
                        <option value="CCE">CCE</option>
                        <option value="Bio Med">Bio Med</option>
                        <option value="Chem">Chem</option>
                        <option value="Mech">Mech</option>
                        <option value="ECE">ECE</option>
                        <option value="EEE">EEE</option>
                        <option value="Civil">Civil</option>
                        <option value="CSBS">CSBS</option>
                        <option value="AIDS">AIDS</option>
                        <option value="Bio Tech">Bio Tech</option>
                        <option value="IT">IT</option>
                    </select>
                </div>
            </div>

            <div>
                <label class="block text-[10px] uppercase text-gray-400 font-semibold tracking-wider mb-2">Set Account Password</label>
                <div class="relative">
                    <input type="password" id="register-password" name="password" required placeholder="enter your password" class="w-full bg-black/40 border border-white/10 rounded-xl pl-4 pr-10 py-3 text-white focus:outline-none focus:border-cyan-400 transition">
                    <button type="button" onclick="togglePasswordVisibility('register-password', this)" class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-white focus:outline-none">
                        <svg class="w-4 h-4 eye-open" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-3-3 3 3 0 013 3z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg>
                        <svg class="w-4 h-4 eye-closed hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.542-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 113.382 3.382m0.792-3.382a3 3 0 11-1.104-1.104m3.52 3.52l3.52 3.52M3 3l18 18"></path></svg>
                    </button>
                </div>
            </div>
            <div>
                <label class="block text-[10px] uppercase text-gray-400 font-semibold tracking-wider mb-2">Confirm Account Password</label>
                <div class="relative">
                    <input type="password" id="register-confirm" required placeholder="confirm your password" class="w-full bg-black/40 border border-white/10 rounded-xl pl-4 pr-10 py-3 text-white focus:outline-none focus:border-cyan-400 transition">
                    <button type="button" onclick="togglePasswordVisibility('register-confirm', this)" class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-white focus:outline-none">
                        <svg class="w-4 h-4 eye-open" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-3-3 3 3 0 013 3z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg>
                        <svg class="w-4 h-4 eye-closed hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.542-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 113.382 3.382m0.792-3.382a3 3 0 11-1.104-1.104m3.52 3.52l3.52 3.52M3 3l18 18"></path></svg>
                    </button>
                </div>
            </div>
            <button type="submit" class="w-full py-3 bg-purple-600 hover:bg-purple-700 text-white rounded-xl text-xs sm:text-sm font-semibold transition mt-6 shadow-lg shadow-purple-600/20 font-mono">
                Complete Registration
            </button>
        </form>
    </div>

    <!-- Interface Controller -->
    <script>
        const cursor = document.getElementById('cursor-core');
        document.addEventListener('mousemove', (e) => {
            if (window.innerWidth > 640) {
                cursor.style.left = `${e.clientX}px`;
                cursor.style.top = `${e.clientY}px`;
            }
        });

        const formSetup = document.getElementById('form-setup');
        const regPassword = document.getElementById('register-password');
        const regConfirm = document.getElementById('register-confirm');
        const regErrorBox = document.getElementById('register-error-box');

        // General password visibility toggle helper function
        function togglePasswordVisibility(inputId, button) {
            const input = document.getElementById(inputId);
            const openIcon = button.querySelector('.eye-open');
            const closedIcon = button.querySelector('.eye-closed');
            
            if (input.type === 'password') {
                input.type = 'text';
                openIcon.classList.add('hidden');
                closedIcon.classList.remove('hidden');
            } else {
                input.type = 'password';
                openIcon.classList.remove('hidden');
                closedIcon.classList.add('hidden');
            }
        }

        // Client-side verification during form submission
        formSetup.addEventListener('submit', (e) => {
            if (regPassword.value !== regConfirm.value) {
                e.preventDefault(); // Stop form submission
                regErrorBox.classList.remove('hidden'); // Display alert box
                regConfirm.focus();
            } else {
                regErrorBox.classList.add('hidden'); // Hide alert box
            }
        });
    </script>
</body>
</html>